UNRECOGNIZED(¿?¿?¿?¿?¿?¿?¿?) 
VOID 
UNRECOGNIZED($$$$$$$$$$$$$$) 
VAR 
LOGICAL_NEG LOGICAL_NEG LOGICAL_NEG LOGICAL_NEG LOGICAL_NEG LOGICAL_NEG LOGICAL_NEG LOGICAL_NEG LOGICAL_NEG LOGICAL_NEG LOGICAL_NEG LOGICAL_NEG LOGICAL_NEG LOGICAL_NEG 
CONST 
COMMA COMMA COMMA COMMA COMMA COMMA COMMA COMMA COMMA COMMA COMMA COMMA COMMA COMMA 
IF 
UNRECOGNIZED(||||||||||||||) 
ELSE 
UNRECOGNIZED(@@@@@@@@@@@@@@) 
WHILE 
MINUS MINUS MINUS MINUS MINUS MINUS MINUS MINUS MINUS MINUS MINUS MINUS MINUS MINUS 
READ 
LINE_COMMENT(//////////////) 
PRINT 
MULT MULT MULT MULT MULT MULT MULT MULT MULT MULT MULT MULT MULT MULT 
FOR 
PLUS PLUS PLUS PLUS PLUS PLUS PLUS PLUS PLUS PLUS PLUS PLUS PLUS PLUS 
FOR 
ID(______________) 
DO 
DIVIDE  DIVIDE  DIVIDE  DIVIDE  DIVIDE  DIVIDE  LINE_COMMENT(//) 
NEQ NEQ NEQ NEQ NEQ NEQ NEQ 
LTE GTE LTE GTE LTE GTE LTE 
LTE ASSIGN GT 
GTE ASSIGN LT 
ASSIGN NEQ NEQ NEQ NEQ NEQ NEQ LOGICAL_NEG 
LT GT LT GT LT GT LT GT LT GT LT GT LT GT 
GT LT GT LT GT LT GT LT GT LT GT LT GT LT 
LT LT GT GT LT LT GT GT LT LT GT GT LT LT 
LOGICAL_NEG ID(a) LOGICAL_NEG UNRECOGNIZED(?) ID(bb) LOGICAL_NEG UNRECOGNIZED(?$) ID(ccc) LOGICAL_NEG UNRECOGNIZED(?$|) ID(dddd) 
ID(email) UNRECOGNIZED(@) ID(domain) UNRECOGNIZED(.) ID(org) 
NUM(998) NEQ NUM(2) 
PLUS MINUS MULT DIVIDE PLUS MINUS MULT DIVIDE PLUS MINUS MULT DIVIDE PLUS MINUS 